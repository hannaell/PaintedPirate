import React from 'react';
import Layout from '../components/Layout';
import CartList from '../components/CartList';

const Cart = () => {
    return(
        <Layout>
            <CartList />
        </Layout>
    );
}

export default Cart;